# util-game

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build util-game` to build the library.

## Running unit tests

Run `nx test util-game` to execute the unit tests via [Jest](https://jestjs.io).
